streamlit>=1.36
docxtpl>=0.16
python-docx>=1.1
num2words>=0.5.13
# docx2pdf drives Microsoft Word (Windows COM) or macOS Word via AppleScript.
# It is not useful on Linux/servers -- pdf_convert.py falls back to LibreOffice
# via `soffice --headless` there. Skipping the install on Linux also avoids
# pulling pywin32 into the Docker image.
docx2pdf>=0.1.8; sys_platform != "linux"
